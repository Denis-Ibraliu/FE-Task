import { TableColumnType } from "../components/ui-components/Table";

const usersTableColumns: TableColumnType[] = [
  { header: "Id", field: "id" },
  { header: "Name", field: "name" },
  { header: "Email", field: "email" },
  { header: "Phone", field: "phone" },
];

export default usersTableColumns;
